# Development dependencies
freezegun==1.4.0             # Time mocking

# API documentation (Served by FastAPI CDN by default)
# swagger-ui-bundle==1.1.0     # Uncomment if offline Swagger UI is needed
